language: ruby

cache: bundler 

before_install: gem update bundler

install:
  - bundle install

before_script:
 - chmod +x ./setup

script:
  - "./setup.sh"

branches:
  only:
  - build

sudo: false